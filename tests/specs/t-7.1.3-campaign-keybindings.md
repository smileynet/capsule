# Test Specification: Add campaign key bindings and help

## Bead: cap-fj8.1.3

## Tracer
Discoverability — proves help bar works for new modes.

## Context
- Extend keys.go with campaign key bindings
- Extend help.go with campaign mode → key map routing

## Test Cases

| Input | Expected Output | Notes |
|-------|-----------------|-------|
| CampaignKeyMap() | Returns key.Map with up/down/tab/abort | Bindings defined |
| CampaignKeyMap().Up | Matches "up", "k" | Vim-style navigation |
| CampaignKeyMap().Down | Matches "down", "j" | Vim-style navigation |
| CampaignKeyMap().Abort | Matches "q", "ctrl+c" | Abort campaign |
| CampaignKeyMap().Tab | Matches "tab" | Focus switch |
| HelpBindings(ModeCampaign) | Returns CampaignKeyMap() | Mode routing |
| HelpBindings(ModeCampaignSummary) | Returns SummaryKeyMap() | Reuse summary keys |

## Edge Cases
- [ ] CampaignKeyMap matches pipeline key patterns (consistency)
- [ ] Help text is concise (fits in help bar width)

## Implementation Notes
- campaignKeys struct mirrors pipelineKeys (same navigation model)
- Help routing: simple switch on Mode, add two new cases
